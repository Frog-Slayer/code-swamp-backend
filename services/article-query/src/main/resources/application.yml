spring:
  application:
    name: codeswamp
  profiles:
    active: dev
  config:
    import: application-secret.yml
  graphql:
    graphiql:
      enabled: true

server:
  forward-headers-strategy: framework
  port: 8083
